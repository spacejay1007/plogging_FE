import React from 'react';
import PostCard from '../components/PostCard';

const Main = (props) => {
  return (
    <React.Fragment>
      <PostCard />
    </React.Fragment>
  );
};

export default Main;
