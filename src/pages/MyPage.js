import React from 'react';
import { MypageForm } from '../components';

const Mypage = (props) => {
  return (
    <React.Fragment>
      <MypageForm />
    </React.Fragment>
  );
};

export default Mypage;
