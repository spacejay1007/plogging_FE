import React from 'react';
import { BookMarkMyForm } from '../components';

const MyBookMark = (props) => {
  return (
    <React.Fragment>
      <BookMarkMyForm />
    </React.Fragment>
  );
};

export default MyBookMark;
